# 🎵 音频响应功能说明

## 功能概述

为首页 Hero 区域添加了实时音频响应功能，可以根据麦克风输入的音频频率动态调整视觉效果。

## 视觉效果响应

### 1. 圆环（Orbits）
- **外圈（o-1）**：响应低频（bass），随节奏放大缩小
- **中圈（o-2）**：响应中频（mid），跟随人声变化
- **内圈（o-3）**：响应高频（treble），跟随高音变化

### 2. 粒子（Particles）
- 根据整体音频强度调整大小和透明度
- 辉光效果随音量增强
- 颜色自动适配明暗主题

### 3. 光斑（Blobs）
- **紫色光斑（b-1）**：响应低频
- **粉色光斑（b-2）**：响应中频
- **蓝色光斑（b-3）**：响应高频

### 4. 节点（Nodes）
- 根据中频强度脉动
- 模拟数据流动效果

### 5. 星点（Sparkles）
- 响应高频，随高音闪烁
- 营造星空效果

## 使用方法

### 启用音频响应
1. 访问首页
2. 点击右下角的"音频响应"按钮
3. 浏览器会请求麦克风权限，点击"允许"
4. 开始播放音乐或说话，视觉效果会随音频变化

### 关闭音频响应
- 再次点击按钮即可关闭
- 所有视觉效果会恢复到默认状态

## 技术实现

### 核心技术
- **Web Audio API**：音频分析
- **MediaStream API**：麦克风访问
- **FFT 分析**：频率分解（fftSize: 256）
- **requestAnimationFrame**：流畅动画

### 频段划分
```javascript
低频（Bass）：   0-10   bins  // 节奏、鼓点
中频（Mid）：    10-40  bins  // 人声、旋律
高频（Treble）： 40-100 bins  // 高音、细节
```

### 性能优化
- 使用 `requestAnimationFrame` 确保 60fps
- 频率数据归一化到 0-1 范围
- 仅在音频启用时运行分析循环
- 组件卸载时自动清理资源

## 浏览器兼容性

### 完全支持
- ✅ Chrome 88+
- ✅ Edge 88+
- ✅ Firefox 85+
- ✅ Safari 14.1+

### 注意事项
- 需要 HTTPS 或 localhost 环境
- 需要用户授权麦克风权限
- 移动端浏览器可能有限制

## 用户体验

### 明色模式
- 玻璃拟态按钮（紫粉渐变）
- 温馨的紫色脉动效果
- 柔和的阴影和辉光

### 暗色模式
- 半透明深色按钮
- 荧光绿色强调
- 科技感辉光效果

### 移动端适配
- 按钮仅显示图标（节省空间）
- 位置调整到右下角
- 触摸友好的尺寸

## 使用场景

### 适合场景
- 🎵 音乐播放时的视觉增强
- 🎤 演讲/直播时的动态背景
- 🎮 互动展示和演示
- 🎨 创意展示和品牌体验

### 建议
- 在安静环境下效果更明显
- 播放节奏感强的音乐效果最佳
- 可以对着麦克风说话测试效果

## 隐私说明

- ✅ 音频数据仅在本地处理
- ✅ 不会录音或保存任何数据
- ✅ 不会上传到服务器
- ✅ 关闭功能后立即停止麦克风访问

## 未来优化方向

1. **预设模式**：添加不同的视觉响应预设
2. **灵敏度调节**：允许用户调整响应强度
3. **音频源选择**：支持选择系统音频而非麦克风
4. **录制功能**：保存音频响应的视觉效果
5. **更多视觉元素**：添加更多可响应的动画元素

## 开发者说明

### 修改文件
- `src/components/home/HeroSection.vue`

### 关键函数
- `initAudio()` - 初始化音频上下文
- `visualize()` - 主可视化循环
- `getAverageFrequency()` - 计算频段平均值
- `updateOrbits()` - 更新圆环效果
- `updateParticles()` - 更新粒子效果
- `updateBlobs()` - 更新光斑效果
- `resetVisuals()` - 重置所有效果

### 调试技巧
```javascript
// 在 visualize() 函数中添加日志
console.log('Bass:', bass, 'Mid:', mid, 'Treble:', treble)
```

---

**创建日期**：2026-05-20  
**版本**：v1.0.0  
**作者**：CNCG Team
