# 飞书权限 JSON 导入指南

> 📅 创建日期：2026-05-07
> 🎯 用途：通过 JSON 批量导入权限，避免手动搜索

---

## 📋 概述

飞书开放平台支持通过 JSON 格式批量导入权限配置，这样可以一次性添加多个权限，无需逐个搜索。

我已经在项目中创建了 `feishu_permissions.json` 文件，包含所有必要的权限。

---

## 🔧 如何导入权限 JSON

### 步骤 1：进入权限管理

1. 访问 [飞书开放平台](https://open.feishu.cn/)
2. 进入您的应用 `CNCG 内容管理系统`
3. 左侧菜单 → **"权限管理"**
4. 查找 **"导入权限"** 或 **"批量添加"** 按钮

### 步骤 2：导入 JSON

有两种方式：

#### 方式 1：复制粘贴 JSON（推荐）

1. 点击 **"导入权限"** 按钮
2. 选择 **"粘贴 JSON"** 或 **"手动输入"**
3. 复制以下 JSON 内容：
4. 粘贴到输入框
5. 点击确认

#### 方式 2：上传 JSON 文件

1. 点击 **"导入权限"** 按钮
2. 选择 **"上传文件"** 或 **"从文件导入"**
3. 选择项目中的 `feishu_permissions.json` 文件
4. 点击上传
5. 确认导入

### 步骤 3：检查导入结果

导入成功后，页面会显示已添加的权限列表。确认以下必需权限已添加。

---

## 📄 feishu_permissions.json 文件内容

```json
{
  "scopes": {
    "tenant": [
      "docx:document:readonly",
      "docx:document.meta:readonly",
      "docx:document.element:readonly",
      "drive:file:readonly",
      "drive:folder:readonly",
      "drive:drive:readonly",
      "wiki:space:readonly",
      "wiki:node:readonly",
      "docx:docs:readonly"
    ],
    "user": [
      "docx:document:readonly",
      "docx:document.meta:readonly"
    ]
  }
}
```

---

## 📋 权限说明

### tenant（租户级权限）

这些权限对所有用户生效，通常用于服务端调用（推荐优先添加）：

| 权限标识 | 说明 | 重要性 |
|---------|------|--------|
| `docx:document:readonly` | 读取文档正文内容 | ⭐⭐⭐⭐⭐ 最重要 |
| `docx:document.meta:readonly` | 读取文档标题等信息 | ⭐⭐⭐⭐ 重要 |
| `docx:document.element:readonly` | 读取文档中的块元素 | ⭐⭐⭐ 推荐 |
| `drive:file:readonly` | 读取云文档文件列表 | ⭐⭐⭐ 推荐 |
| `drive:folder:readonly` | 读取云文档文件夹 | ⭐⭐ 推荐 |
| `drive:drive:readonly` | 云文档只读权限 | ⭐⭐ 推荐 |
| `wiki:space:readonly` | 读取知识库空间信息 | ⭐⭐⭐ 推荐 |
| `wiki:node:readonly` | 读取知识库节点信息 | ⭐⭐⭐ 推荐 |
| `docx:docs:readonly` | 文档库只读权限 | ⭐⭐ 推荐 |

### user（用户级权限）

这些权限需要用户授权，通常用于用户访问自己的文档：

| 权限标识 | 说明 | 重要性 |
|---------|------|--------|
| `docx:document:readonly` | 读取文档正文内容 | ⭐⭐⭐⭐⭐ 重要 |
| `docx:document.meta:readonly` | 读取文档标题等信息 | ⭐⭐⭐⭐ 重要 |

---

## ⚠️ tenant vs user 权限的区别

### tenant 权限

- **特点**：应用级别的权限，不需要用户授权
- **使用场景**：服务端调用，后台任务
- **优势**：不需要用户登录授权
- **推荐**：优先添加 tenant 权限

### user 权限

- **特点**：需要用户明确授权才能使用
- **使用场景**：用户访问自己的私有文档
- **要求**：需要 OAuth 2.0 用户授权流程
- **推荐**：根据需要添加

---

## ⚠️ 注意事项

### 1. 权限可能重复

如果某些权限已经添加过，导入时会自动跳过或提示冲突，这是正常的。

### 2. 需要审批

某些高危权限可能需要管理员审批才能生效。如果是个人版，通常会自动通过。

### 3. 应用需要发布

导入权限后，**必须发布应用版本**才能生效。具体步骤：

1. 左侧菜单 → **"版本管理与发布"**
2. 点击 **"创建版本"**
3. 填写：
   ```
   版本号：1.0.0
   更新说明：添加飞书文档读取权限
   ```
4. 点击 **"申请发布"**
5. 等待审批通过（通常很快）

### 4. 查看导入状态

在权限管理页面，可以查看每个权限的状态：

| 状态 | 说明 | 颜色 |
|------|------|------|
| 已添加 | 权限已成功添加 | 绿色 ✓ |
| 待审批 | 等待管理员审批 | 黄色 ⏰ |
| 审批拒绝 | 管理员拒绝了该权限 | 红色 ✗ |

---

## 🔍 如果找不到"导入权限"按钮

有些版本的飞书开放平台可能不支持 JSON 导入，您可以尝试以下方法：

### 方法 1：手动搜索

在权限搜索框中逐个搜索以下权限标识（不需要输入完整名称）：

**搜索关键词**：
- `docx`
- `document`
- `drive`
- `wiki`
- `readonly`

### 方法 2：查看推荐权限

在权限管理页面，查找"推荐权限"或"常用权限"部分。

### 方法 3：使用 API 测试

通过 API 调用测试是否需要特定权限，API 会返回缺少的权限信息。

---

## 📝 权限验证

导入权限后，可以通过以下方式验证：

### 方法 1：测试页面验证

1. 启动开发服务器：
   ```bash
   npm run dev
   ```

2. 访问测试页面：
   ```
   http://localhost:5173/feishu-test
   ```

3. 点击"测试连接"按钮

4. 查看连接状态和错误信息

### 方法 2：命令行验证

使用 curl 测试 Token 获取：

```bash
curl -X POST 'https://open.feishu.cn/open-apis/auth/v3/tenant_access_token/internal' \
  -H 'Content-Type: application/json' \
  -d '{
    "app_id": "cli_a97693b0d3381cba",
    "app_secret": "你的App Secret"
  }'
```

如果返回 `code: 0`，说明应用凭证正确。

### 方法 3：API 测试

测试获取文档内容：

```bash
# 先获取 Token
TOKEN="你的Token"

# 测试获取文档
curl -X GET 'https://open.feishu.cn/open-apis/docx/v1/documents/你的文档ID' \
  -H 'Authorization: Bearer '$TOKEN
```

如果返回权限错误，会明确说明缺少哪个权限。

---

## 🛠️ 常见问题

### Q1: JSON 导入按钮在哪里？

**回答**：不同版本的飞书开放平台界面可能不同。通常在：

- 权限管理页面的右上角
- 权限列表的上方或下方
- 可能标有"批量导入"、"导入权限"、"JSON 导入"等文字

### Q2: 导入的权限不完整？

**回答**：可能的原因：

1. **JSON 格式错误**：检查 JSON 语法是否正确
2. **权限标识不存在**：某些权限可能在您的应用类型中不可用
3. **需要更高权限级别**：某些权限需要更高级别的企业账号

### Q3: 导入失败怎么办？

**回答**：

1. **检查 JSON 格式**：确保是有效的 JSON 格式
2. **逐个添加**：如果批量导入失败，尝试手动逐个添加权限
3. **查看错误信息**：飞书会显示具体的错误原因

### Q4: 所有权限都需要审批吗？

**回答**：不一定。权限分为几种：

- **无需审批**：基础只读权限，通常自动通过
- **需要审批**：涉及敏感操作的权限
- **需要付费**：高级权限需要企业付费版

### Q5: 权限导入后多久生效？

**回答**：

- **无需审批的权限**：通常**立即生效**
- **需要审批的权限**：通常**1-24小时**
- **应用发布后**：部分权限需要发布应用版本后才生效

---

## 📋 导入检查清单

导入权限前，请确认：

- [ ] 已经在飞书开放平台打开应用
- [ ] 进入应用的"权限管理"页面
- [ ] 找到"导入权限"或"批量添加"功能
- [ ] 准备好 JSON 文件内容
- [ ] 了解需要导入哪些权限

导入时：

- [ ] 复制完整的 JSON 内容
- [ ] 确认 JSON 格式正确
- [ ] 逐个确认导入的权限
- [ ] 处理任何错误或冲突

导入后：

- [ ] 检查权限列表是否完整
- [ ] 确认必需权限已添加（tenant 和 user 都要有）
- [ ] 申请发布应用版本
- [ ] 等待审批通过
- [ ] 测试权限是否生效

---

## 🎯 推荐的最小权限集

如果只想快速测试，可以只导入以下必需权限：

```json
{
  "scopes": {
    "tenant": [
      "docx:document:readonly"
    ],
    "user": [
      "docx:document:readonly"
    ]
  }
}
```

验证这个权限能工作后，再逐步添加其他权限。

---

## 📚 相关资源

- [飞书开放平台权限说明](https://open.feishu.cn/document/uAjLw4CM/ukTMukTMukTM/reference/application-v6/permission/description)
- [飞书文档 API 权限](https://open.feishu.cn/document/server-docs/docs/docs/docx-v1/document)
- [飞书知识库 API 权限](https://open.feishu.cn/document/server-docs/docs/wiki-v2/space)

---

## 💬 需要帮助？

如果在导入过程中遇到问题：

1. **截图错误信息**：将完整的错误信息截图
2. **说明操作步骤**：描述您执行了哪些操作
3. **提供 JSON 内容**：如果有修改过 JSON，附上内容

**我会帮您解决！** 💪

---

**祝您权限配置顺利！** 🚀
