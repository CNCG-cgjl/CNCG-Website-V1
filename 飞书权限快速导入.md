# 🚀 飞书权限快速导入指南

> 📅 创建日期：2026-05-07
> ⏱️预计时间：3-5 分钟

---

## ⚡ 快速步骤

### Step 1: 打开权限管理

1. 访问 [飞书开放平台](https://open.feishu.cn/)
2. 进入应用 **CNCG 内容管理系统**
3. 左侧菜单 → **"权限管理"**

### Step 2: 导入 JSON

1. 在权限管理页面找到 **"导入权限"** 或 **"批量添加"** 按钮
2. 选择 **"粘贴 JSON"** 或 **"从文件导入"**
3. 复制以下内容：

```json
{
  "scopes": {
    "tenant": [
      "im:message:send_as_bot",
      "docx:document:readonly"
    ],
    "user": [
      "docx:document:readonly"
    ]
  }
}
```

4. 点击确认

### Step 3: 发布应用

1. 左侧菜单 → **"版本管理与发布"**
2. 点击 **"创建版本"**
3. 填写：
   ```
   版本号：1.0.0
   更新说明：添加飞书文档读取权限
   ```
4. 点击 **"申请发布"**
5. 等待发布成功（通常很快）

### Step 4: 测试连接

1. 启动开发服务器：
   ```bash
   npm run dev
   ```

2. 访问：
   ```
   http://localhost:5173/feishu-test
   ```

3. 查看连接状态

---

## 📋 权限说明

### tenant（租户级权限）

| 权限标识 | 说明 |
|---------|------|
| `im:message:send_as_bot` | 发送消息（用于机器人） |
| `docx:document:readonly` | 读取文档正文内容（必需） |

### user（用户级权限）

| 权限标识 | 说明 |
|---------|------|
| `docx:document:readonly` | 读取文档正文内容 |

---

## ⚠️ 注意事项

### 1. 必需权限

| 权限 | 级别 | 说明 |
|------|------|------|
| `docx:document:readonly` | tenant + user | 读取文档正文（最重要） |

### 2. 其他权限

如果后续需要更多权限，可以在飞书开放平台的权限管理页面中手动搜索添加。

---

## ✅ 验证清单

权限配置完成后的检查项：

- [ ] tenant 权限包含 `docx:document:readonly`
- [ ] user 权限包含 `docx:document:readonly`
- [ ] 应用版本已发布
- [ ] 发布状态显示成功
- [ ] 测试页面显示连接成功
- [ ] 可以获取文档内容

---

## 🎯 下一步

权限配置成功后：

1. **创建测试文档**
   - 在飞书中创建一个文档
   - 记录文档 Token

2. **配置 .env.local**
   ```bash
   VITE_FEISHU_APP_ID=cli_a97693b0d3381cba
   VITE_FEISHU_APP_SECRET=你的App Secret
   ```

3. **开始使用飞书知识库**

---

**完成权限配置后告诉我，我将帮您继续下一步！** 🚀
